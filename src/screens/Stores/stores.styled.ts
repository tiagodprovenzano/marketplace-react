import styled, { css } from "styled-components";

export const StoresWrapper = styled.div`${() => css`
  display: flex;
  flex-direction: row-reverse;
  justify-content: space-between;
  flex-grow: 1;
  padding: 10px;
`}`