import styled, {css} from "styled-components";

const ScreenViewWrapper = styled.div`${()=> css`
    .main-wrapper{
        display: flex;
        min-height: 100vh;
    }

`}`

export default ScreenViewWrapper